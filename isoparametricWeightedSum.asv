function weightedSum = isoparametricWeightedSum(param_coordinates, node_coordinates)
    xi = param_coordinates(1);
    eta = param_coordinates(2);
    zeta = param_coordinates(3);

    weightedSum=shapeFunctionsHexahedral(xi, eta, zeta);

end

